# Writer checkpoint: Zmey

This file is an item-level checkpoint, not a GPT-generated article.

The configured writer command was attempted twice with `APIMART_MODEL=gpt-5-mini`:

```text
APIMart returned an HTTP error: invalid API key
```

Both attempts failed before article output was produced. Source-backed research is in `research.md`; the prepared guide page is in `src/content/docs/gameplay/zmey.md`. The item should be marked blocked by the Integrator unless the writer credentials are repaired under the single-item retry policy.
